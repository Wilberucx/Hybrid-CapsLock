; ==============================================================================
; HYBRID CAPSLOCK - GENERAL CONFIGURATION
; ==============================================================================
; This file contains general settings for HybridCapsLock that don't belong
; to specific layers. Layer-specific configurations are in separate files:
; - programs.ini (Programs Layer)
; - timestamps.ini (Timestamps Layer)
; ==============================================================================

[General]
; General application settings
; Currently no general settings needed - all moved to layer-specific files

; ==============================================================================
; FUTURE SETTINGS
; ==============================================================================
; Add general settings here as needed (e.g., UI preferences, global timeouts)
; ==============================================================================